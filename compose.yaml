services:
  recallops:
    build: .
    ports:
      - "8000:8080"
    environment:
      APP_ENV: local
      STATE_BACKEND: local
      AGENT_RUNTIME_MODE: deterministic
      DEMO_API_KEY: local-demo
    volumes:
      - ./seed:/app/seed:ro
      - ./scenarios:/app/scenarios:ro
